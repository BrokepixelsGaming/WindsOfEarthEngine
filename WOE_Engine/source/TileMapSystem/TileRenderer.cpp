// TileRenderer.cpp
/*
#include "TileRenderer.h"
#include "..\3rdParty\include\SDL\SDL.h"

//---------------------------------------------------------------------------------------------------------------------
// Constructor.
//---------------------------------------------------------------------------------------------------------------------
TileRenderer::TileRenderer(unsigned char red, unsigned char green, unsigned char blue, unsigned char alpha)
    : m_red(red)
    , m_green(green)
    , m_blue(blue)
    , m_alpha(alpha)
{
    //
}

//---------------------------------------------------------------------------------------------------------------------
// Renders the tile.
//      -pRenderer: The SDL renderer to use.
//      -rect:      The SDL rect describing the size and position of the tile.
//---------------------------------------------------------------------------------------------------------------------
void TileRenderer::Render(SDL_Renderer* pRenderer, const SDL_Rect& rect)
{
    SDL_SetRenderDrawColor(pRenderer, m_red, m_green, m_blue, m_alpha);
    SDL_RenderFillRect(pRenderer, &rect);
}

*/